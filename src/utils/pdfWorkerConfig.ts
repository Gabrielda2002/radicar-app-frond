// Configuración del worker de PDF.js para producción
declare global {
  interface Window {
    pdfWorkerSrc?: string;
  }
}

// Configuración del worker de PDF.js para producción
declare global {
  interface Window {
    pdfWorkerSrc?: string;
  }
}

// Configuración del worker de PDF.js para producción
// Solo para resolver problemas de MIME type en producción

// Esta configuración será aplicada solo cuando sea necesario
// pdfslick manejará automáticamente la versión correcta del worker

export {};
